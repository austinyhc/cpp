#include <iostream>
#include <vector>
#include <optional>
//#include "solution.h"

class Solution {
public:
    std::optional<std::vector<int>> twoSum(std::vector<int> valuse) {
        return {};
    }
};

int main() {
    std::vector<int> values {1, 2, 9, 8};

    for (auto v : values) {
        std::cout << v << std::endl;
    }

    return 0;
}
